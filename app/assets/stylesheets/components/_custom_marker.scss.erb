
.custom-marker {
  position: relative;
  width: 75px;   /* Marker size */
  height: 75px;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Marker image */
.marker-image {
  width: 65px;
  height: 65px;
  background-image: url('<%= asset_path("pin_mapbox.png") %>'); /* Rails asset pipeline */
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}
